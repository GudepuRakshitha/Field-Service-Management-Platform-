# Database Settings (Local Docker Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=keystone
DB_USER=keystone
DB_PASSWORD=keystone123

# Cloud Database Connection Option (Supabase IPv4 Connection Pooler)
# Note: Use Supabase Pooler (port 6543) instead of direct host (5432) because cloud deployment environments (Render/Koyeb) are IPv4-only.
# SPRING_DATASOURCE_URL=jdbc:postgresql://aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require
# DB_USER=postgres
# DB_PASSWORD=[YOUR-SUPABASE-DB-PASSWORD]

# JWT Secret (Base64 / 32+ character HMAC key)
KEYSTONE_JWT_SECRET=v9y$B&E)H@MbQeThWmZq4t7w!z%C*F-JaNdRfUjXn2r5u8x/A?D(G+KbPeShVkYp

# MailHog SMTP Settings
SMTP_HOST=localhost
SMTP_PORT=1025
